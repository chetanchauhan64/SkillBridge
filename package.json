{
  "name": "fiverr-app",
  "version": "1.0.0",
  "description": "Fiverr marketplace clone deployment root",
  "main": "backend/server.js",
  "scripts": {
    "install": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend && npm run generate --prefix backend && npm run push --prefix backend",
    "start": "npm start --prefix backend"
  }
}
